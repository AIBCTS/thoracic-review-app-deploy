.streamlit/
secrets/
.secrets/
*.pyc
__pycache__/
*.json

# CSV files — reviewer data should not be in git
*.csv

.DS_Store
.venv/
env/
venv/
data_llm/
archive/
_archive/

# Old data folder (to be deleted, do not track)
data_/

# PDFs are pushed once but not version-controlled (use git add -f to add)
data/*.pdf

# reports/ is intentionally tracked in git
!reports/
